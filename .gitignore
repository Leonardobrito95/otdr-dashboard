# Segredos
.env
TOKEN API META.txt

# Ambiente Python
venv/
__pycache__/
*.pyc

# Estado/cache em runtime (regenerado pelo sistema, não é código-fonte)
cache_onus.json
saude_estado.json
synkr_avisos.json

# Logs
logs/
*.log

# Backups de deploy manual (o próprio git substitui essa necessidade)
*.bak*

# Sem permissão de leitura pro usuário canaa (root-only, 700) — adicionar
# manualmente depois de ajustar a permissão, se quiser versionar a logo.
dashboard/static/logo.png
